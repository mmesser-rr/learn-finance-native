type RecentTransaction @model #(mutations: null) @auth(rules: [{ allow: owner }]) 
{
  transactionId: String! 
  athleteId: String!
  status: String!
  amount: Float
  idempotencyKey: String
  direction: String
  createdAt: String
  read: Boolean
  settled: String
  podAllocation: PodSettings
}

#@auth(rules: [{ allow: owner }]) 